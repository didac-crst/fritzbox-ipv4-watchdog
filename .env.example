# --- FRITZ!Box creds / host ---------------------------------
FRITZ_HOST=fritz.box
FRITZ_USER=svc-rebooter
FRITZ_PASSWORD=REPLACE_ME

# --- Behaviour ----------------------------------------------
TARGET_SVC=WANPPPConnection1      # use your PPP object
CHECK_EVERY_SEC=60
MAX_BAD_CYCLES=10                 # 10×60s = 10 minutes

# --- Logging -------------------------------------------------
LOG_LEVEL=DEBUG                   # start verbose; later: INFO or WARNING
LOG_DIR=/logs
LOG_FILE=watchdog.log
LOG_ROTATE_WHEN=midnight          # or: S, M, H, D, W0..W6
LOG_ROTATE_INTERVAL=1             # how many units per rotation
LOG_BACKUP_COUNT=30               # keep last 30 files
LOG_STDOUT=true                   # also log to container stdout
LOG_JSON=false                    # true = JSON logs (optional)
LOG_EVERY_CYCLE=false             # true = log every poll; false = only state changes

# --- Timezone (optional, affects timestamps shown) -----------
TZ=Europe/Berlin